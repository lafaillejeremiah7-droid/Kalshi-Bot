name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements.txt
      - name: Validate Linux terminal scripts
        run: bash -n bin/xau scripts/install-linux.sh
      - name: Compile Python
        run: python -m compileall -q main.py xau_company tests
      - name: Run tests
        run: |
          set +e
          python -m pytest -q 2>&1 | tee test-output.txt
          status=${PIPESTATUS[0]}
          exit $status
      - name: Upload test diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pytest-output
          path: test-output.txt
